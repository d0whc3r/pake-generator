name: setup
description: pnpm, node and the project dependencies

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
    - uses: actions/setup-node@v7
      with:
        cache: pnpm
        node-version-file: .nvmrc
    - run: pnpm install --frozen-lockfile
      shell: bash
